<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lotusapps.kamaljudge.supplemetationapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SecondMainActivity" />
        <activity android:name=".SignIn" />
        <activity android:name=".SignUp" />
        <activity android:name=".ProductsActivity" />
        <activity android:name=".ProductDescriptionActicity" />
        <activity android:name=".CartActivity" />
        <activity android:name=".PlacedOrdersActivity" />
        <activity android:name=".PlaceOrderActivity" />
        <activity android:name=".BMIActivity"></activity>
    </application>

</manifest>